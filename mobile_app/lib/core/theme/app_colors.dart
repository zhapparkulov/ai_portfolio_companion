import 'package:flutter/material.dart';

/// Stitch UI fallback tokens for a premium fintech mobile interface.
abstract final class AppColors {
  static const primary = Color(0xFF1A1F36);
  static const secondary = Color(0xFF007AFF);
  static const tertiary = Color(0xFF34C759);

  static const background = Color(0xFFF9FAFB);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceMuted = Color(0xFFF2F4F7);
  static const surfaceElevated = Color(0xFFF2F7FF);

  static const textPrimary = Color(0xFF111827);
  static const textSecondary = Color(0xFF6B7280);
  static const textMuted = Color(0xFF9CA3AF);
  static const textInverted = Color(0xFFFFFFFF);

  static const border = Color(0xFFE5E7EB);
  static const positive = Color(0xFF16A34A);
  static const positiveSoft = Color(0xFFEAF8EF);
  static const negative = Color(0xFFC8333A);
  static const negativeSoft = Color(0xFFFEE2E2);
  static const warning = Color(0xFFF59E0B);
  static const infoSoft = Color(0xFFEEF6FF);

  static const shadow = Color(0x1A111827);
}
